import * as React from "react";
import useStyles from "./TsxTemplate.styles";
import { ViewPropTypes } from "./TsxTemplate.types";

const TsxTemplateView = (props: ViewPropTypes) => {
  const { model, classes } = props;
  return <div></div>;
};

export default TsxTemplateView;
